{
  "type": "object",
  "properties": {
    "bot_token": {
      "type": "string",
      "description": "Discord bot token (Bot User OAuth Token from the Discord Developer Portal). Stored plaintext in SQLite — masked in UI; do not commit to env or config.json.",
      "secret": true
    },
    "allowed_dm_user_ids": {
      "type": "string",
      "description": "Comma-separated Discord user IDs allowed to DM the bot. Empty = no DM access. IDs are 17-19 digit snowflakes from Discord's Developer Mode 'Copy ID'.",
      "default": ""
    },
    "allowed_channel_keys": {
      "type": "string",
      "description": "Comma-separated <guild_id>:<channel_id> pairs where the bot will respond to @-mentions. Empty = no channels. Use /gurney enable inside Discord to opt a channel in interactively.",
      "default": ""
    },
    "rate_limit_per_minute": {
      "type": "number",
      "description": "Max user-initiated turns per minute per Discord user. Defends against runaway loops and noisy clients.",
      "default": 10
    },
    "idle_disconnect_minutes": {
      "type": "number",
      "description": "Disconnect the Discord gateway after this many minutes with no inbound messages (0 = stay connected). Useful on Pi targets.",
      "default": 0
    }
  },
  "required": ["bot_token"]
}
