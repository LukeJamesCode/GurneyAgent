{
  "type": "object",
  "properties": {
    "searxng_url": {
      "type": "string",
      "description": "Optional SearXNG instance URL (e.g. https://searx.example.com). Falls back to DuckDuckGo if unset."
    },
    "max_results": {
      "type": "number",
      "default": 5,
      "description": "Default number of results to return per search."
    }
  }
}
