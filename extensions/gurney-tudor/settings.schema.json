{
  "type": "object",
  "properties": {
    "default_generator": {
      "type": "string",
      "enum": ["local", "codex"],
      "default": "local",
      "description": "Which model builds new courses by default. 'local' uses your Ollama/qwen model (free, slower); 'codex' uses gurney-codex if it's enabled and authed (faster, spends your daily Codex budget). Codex falls back to local automatically if it's unavailable."
    },
    "default_depth": {
      "type": "string",
      "enum": ["quick", "standard", "deep"],
      "default": "standard",
      "description": "How many modules and lessons a new course aims for. 'quick' is fastest to generate; 'deep' is the most thorough but takes the longest on local models."
    },
    "use_websearch": {
      "type": "boolean",
      "default": false,
      "description": "Research the topic on the web before building a course, for fresher and more accurate material. Requires the gurney-websearch extension to be installed and enabled; ignored otherwise."
    },
    "use_web_images": {
      "type": "boolean",
      "default": true,
      "description": "When web research is enabled, pull useful images from the same source pages and ask the local multimodal model to verify them for the current lesson segment."
    }
  }
}
