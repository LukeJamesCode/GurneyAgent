# Getting started

The end-to-end install + flash + test walkthrough lives at [`extensions/gurney-speaker/SETUP.md`](../../extensions/gurney-speaker/SETUP.md). That covers both the server-side extension setup and the firmware build/flash steps in the order you'll actually do them.

This `firmware/gurney-speaker/` directory is just the embedded-firmware half. For the structural overview and hardware target list see [`README.md`](README.md).
