# ESP-IDF build artifacts
build/
managed_components/
dependencies.lock
sdkconfig
sdkconfig.old

# clangd index — generated by the VS Code extension, machine-specific
.clangd
.cache/

# Provisioned NVS image — contains WiFi password + device shared secret in
# plaintext. NEVER commit this. Regenerate per-device via tools/provision.py.
nvs.bin

# Per-machine editor settings (paths to local IDF install, COM ports, etc.)
.vscode/settings.json
